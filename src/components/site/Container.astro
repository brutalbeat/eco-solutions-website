---
import type { TagName } from '../../../types/dom';

export interface Props {
  as?: TagName;
  class?: string;
}

const { as: Tag = 'div', class: className = '' } = Astro.props;
---

<Tag class={`mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>
